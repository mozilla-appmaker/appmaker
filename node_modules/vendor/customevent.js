// Polyfill from https://developer.mozilla.org/en-US/docs/Web/API/CustomEvent
function CustomEventPolyfill ( event, params ) {
  params = params || { bubbles: false, cancelable: false, detail: undefined };
  var evt = document.createEvent( 'CustomEvent' );
  evt.initCustomEvent( event, params.bubbles, params.cancelable, params.detail );
  return evt;
}

CustomEventPolyfill.prototype = global.Event.prototype;

module.exports = typeof global.CustomEvent === 'function'
  ? global.CustomEvent
  : global.CustomEvent = CustomEventPolyfill;
